dist: xenial
language: python
sudo: false
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
# command to install dependencies
install:
  - pip install pycodestyle
# command to run tests
script:
  - pycodestyle
